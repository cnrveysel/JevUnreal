# Screenshots & Demo

Capture these once the plugin compiles and runs in the editor:

1. `jev-blueprint-node.png` — Blueprint graph: `Begin Play → Jev Yes / No → On Success → Branch on Answer`, with State/Question pins filled.
2. `jev-demo.gif` (optional) — Play-in-editor clip showing a decision firing with `LogJev` debug output.

No screenshots are fabricated here; placeholders will be replaced with real captures.
