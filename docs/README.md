# Screenshots & Demo

Planned assets for a future documentation update:

1. `jev-blueprint-node.png` — Blueprint graph: `Begin Play → Jev Yes / No → On Success → Branch on Answer`, with State/Question pins filled.
2. `jev-demo.gif` (optional) — Play-in-editor clip showing a decision firing with `LogJev` debug output.

The v0.1 plugin has been tested in Unreal Engine 5.8, including a manual Play In Editor request to the real TypeSafe API. No screenshots or demo clips are included in this release.
